pipeline {

    agent any

    stages {

        stage("Build"){
            steps {
                sh "echo 'Building the Application...'"
            }
        }

        stage("Docker"){
            steps {
                sh "echo 'Dockerizing the Application...'"
            }
        }

        
    }
}